<UserControl x:Class="HRIS.View.HeatMapTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:HRIS.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.Resources>
		<ObjectDataProvider x:Key="RowList"
		                    ObjectInstance="{StaticResource HeatMapsController}"
		                    MethodName="GetRows" />
	</UserControl.Resources>

	<view:ColorGridControl Rows="{Binding Source={StaticResource RowList} }" />
</UserControl>
